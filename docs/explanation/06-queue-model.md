# Queue Model

The pending queue is NDJSON. Entries record pair, action, relative path, source path, directory flag, size, modified time, and timestamp. Effective queue display collapses repeated events.
