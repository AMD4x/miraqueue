# Overview

MiraQueue V1.0.0 is a single-script Windows backup utility. It separates detection from application: Watch mode records filesystem changes into a queue, Preview Pending shows effective work, and Apply Pending performs selected actions. Full Mirror is a separate whole-tree comparison workflow powered by robocopy.
