# Startup Runtime Version

Startup begins with the `Mode` parameter and optional `NoPause`. The script sets UTF-8 output, resolves its own path, derives config/runtime paths, and uses V1.0.0 in generated configuration.
