# Configuration

`New-DefaultConfig` defines defaults. `Initialize-App` creates or loads config, repairs missing fields, expands environment variables, creates the data folder, and prepares queue/log files.
